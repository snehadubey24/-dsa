<h1> Given an array arr[] of size n.Output sum of each subarray of the given array.

for example:arr[]={1,2,2}
output=1,3,5,2,4,2

#include<bits/stdc++.h>

using namespace std;

int main() {
    int n,sum=0;
    cin>>n;
    int arr[n];
    for(int i=0;i<n;i++)
    {
        cin>>arr[i];
    }
    for(int i=0;i<n;i++)
    {  sum=0;
        for(int j=i;j<n;j++)
        {
            sum+=arr[j];
            cout<<sum<<" ";
        }
    }
 return 0;
}
